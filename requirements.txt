numpy>=2.0
rank-bm25>=0.2.2
sentence-transformers>=6.0.1

# Optional — only for the Analysis tab, and only when it has to read a decision
# PDF. The full build normally analyzes straight from the vector index instead,
# and the model endpoint itself needs nothing beyond the standard library.
pypdf>=5.0
