numpy>=2.0
rank-bm25>=0.2.2
sentence-transformers>=6.0.1

# pypdf is needed by scripts/vectorize.py to read the decision PDFs, and by the
# Analysis tab when it has to fall back to a PDF. The Analysis tab normally
# reads the vector index instead, and the model endpoint itself needs nothing
# beyond the standard library.
pypdf>=5.0

# scripts/vectorize.py needs numpy (above) for the embed stage. It needs
# sentence-transformers (above) ONLY for --embedder local; with --embedder api
# it talks to an OpenAI-compatible /v1/embeddings endpoint over the standard
# library alone.
